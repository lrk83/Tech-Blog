<ul class="post-list">
  {{#each posts}}
  <li>
    <article class="post">
        <div class="title">
            <h2>{{title}}</h2>
        </div>
        <div class="date">
            by {{user.username}} on {{format_date created_at}}
        </div>
    </article>
  </li>
  {{/each}}
</ul>